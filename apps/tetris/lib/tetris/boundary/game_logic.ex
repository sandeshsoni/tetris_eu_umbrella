defmodule Tetris.Boundary.GameLogic do
  alias Tetris.Core.{Board, Shape}

  def rotate(game) do
  end

  def move(game, move_direction) do
    # score
    # active_shape
    # next_shape
    # board
  end

end
